# Backend environment variables for local development outside Docker
# This file is used when running the backend directly with `uv run`

# PostgreSQL database URL for SQLAlchemy/SQLModel
# Format: postgresql+psycopg://username:password@host:port/database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/collectapp

# JWT secret key for authentication
# Generate a secure random key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=development_secret_key

# Optional: Upload directory for attachments (default: ./uploads)
# UPLOADS_DIR=./uploads

# Optional: CORS origins for development
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
