# ===========================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# Copy this file to .env and customize
# ===========================================

# Database Configuration
# For Docker: use the db service hostname
# For local dev: use localhost with your local PostgreSQL
DATABASE_URL=postgresql+psycopg://collectapp:collectapp_dev_password@db:5432/collectapp

# Backend Configuration
# Generate a secure random key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-key-change-in-production

# Frontend Configuration
# URL where the backend API is accessible (must include /api prefix for backend routes)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# ===========================================
# PRODUCTION NOTES
# ===========================================
# For production deployment:
# 1. Change SECRET_KEY to a strong random value
# 2. Use managed PostgreSQL or secure database credentials
# 3. Update DATABASE_URL with production database
# 4. Set NEXT_PUBLIC_API_URL to your production domain
# 5. Consider using environment-specific files or secrets management
